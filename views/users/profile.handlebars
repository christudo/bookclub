<h1 class="mt-2 mb-2">{{currentUser.name}}</h1>

My booklist:
<ul>
  {{#each booksUser}}
    <li>
      <a href="/books/show/{{bookId}}">
        {{title}} -- {{this.readStatus}}
      </a>
    </li>
  {{/each}}
</ul>
